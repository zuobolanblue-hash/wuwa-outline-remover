# 安全说明 / Security Policy

## 这个工具会碰什么、不会碰什么

**会碰**（只有这些）：

| 目标 | 动作 |
|---|---|
| `…\Mods\**\Meshes\Color.buf` | 把每个顶点的 R（描边遮罩）、G（描边粗细）两字节清零；可选把 A 清零。**B（皮肤遮罩）永远不动** |
| `…\Meshes\Color.buf.orig.<hash8>.bak` | 新建：改之前的原始文件，按内容哈希命名，只增不改 |
| `…\Meshes\.nooutline.json` | 新建/更新：记录原始哈希、补丁哈希、Alpha、时间、顶点数 |

**不会碰**：游戏本体文件、游戏进程、注册表、系统目录、网络、其它 mod 的文件；
不注入、不 hook、不加载驱动，全程离线。删除/清理操作只会删**它自己创建**的存档与清单
（`-PurgeOrphans` 也只清"对应 mod 已经不存在"的零散产物，绝不碰 Wuwa Mod Fixer 的 `.BAK`）。

## 权限

不需要管理员权限，不写系统盘。唯一的网络行为：没有。

## 数据安全设计

* 每次改动前必定先留原始档；重复运行不会叠加修改（幂等）
* `-Verify` 只读体检：记录是否完整、原始档有没有丢、有没有无主产物
* 还原后与原文件**逐字节一致**（SHA1 校验，见 `tools\wuwa-test.ps1` 第 1 级）
* `tools\wuwa-snapshot.ps1` 提供一套独立于本工具存档的兜底快照

## 报告问题

发现安全问题时，请**不要开公开 issue**，用 GitHub 的私密报告通道：

<https://github.com/zuobolanblue-hash/wuwa-outline-remover/security/advisories/new>

如果只是想反馈"改完没效果 / 备份没列出来"这类使用问题，直接开 issue 就行（见 issue 模板）。

## 支持的版本

只维护最新 release。老版本请先升级再复现。

---

**免责声明**：本工具是第三方非官方工具，与库洛游戏（Kuro Games）及《鸣潮》官方无任何关系，
不含任何游戏素材（图标由 `tools\make-icon.ps1` 用代码绘制）。它只修改**你自己安装的 mod 文件**，
不修改游戏本体。使用前请自行评估所属平台/社区的使用条款。
